rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /service_requests/{doc} {
      allow read, write: if true;
    }
    match /contact_messages/{doc} {
      allow read, write: if true;
    }
  }
}